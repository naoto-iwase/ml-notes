---
title: "Harness Optimization"
description: "LLM agentのharnessを改善し、確かな性能向上を見分ける"
date: "2026-08-08"
date-modified: last-modified
author:
  name: "Naoto Iwase"
  url: "https://iwase.dev"
categories: [LLM, AI Agent]
image: "images/dgm-concept.png"
toc: false
---

大規模言語モデル（large language model, LLM）のエージェント（agent）のふるまいは、モデル（model）を選んだ時点ではまだ決まらない。何をコンテキスト（context）に入れ、どのツール（tool）をどう渡し、いつ検証し、失敗からどう立て直すか。同じモデルでも、周囲の実行系しだいで、解けるタスク（task）、コスト（cost）、失敗モード（failure mode）は変わる。本書では、プロンプト（prompt）、メモリ（memory）、制御フロー（control flow）、検証器（validator）、復旧ロジック（recovery logic）からなるこの実行系を **実行可能ハーネス（executable harness）** と呼び、以下ではハーネス（harness）と略す。

では、モデルを変えずに、実行記録（trace）と評価からハーネス自体を改善できないだろうか。本書は、プロンプト、ワークフロー（workflow）、メモリ、実行時コード（runtime code）の編集を一つの最適化ループ（optimization loop）として捉える。失敗から更新候補（candidate）を作り、限られた予算（budget）とばらつきのある評価の下で本当の改善を見分け、確かめた変更だけを次の実行へ持ち越す。目標は、エージェントが変化すること自体ではない。その変化を、再現可能で信頼できる改善にすることである。
